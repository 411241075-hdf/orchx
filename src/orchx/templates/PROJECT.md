# PROJECT.md — контекст проекта для orchX-агентов

> Этот файл прочитают **все** роли роя orchX (planner, implementer, debugger,
> reviewer, merger, tester, architect) перед началом работы. Здесь надо
> кратко и плотно описать тебе твой проект так, чтобы LLM-агент мог
> ориентироваться без додумывания.
>
> Файл опционален, но **сильно повышает качество работы роя** — без него
> модели приходится самой читать `README.md`, `pyproject.toml`, ходить по
> деревьям, и она часто промахивается с конвенциями.
>
> Замени все placeholder'ы (`<…>`) и удали неактуальные разделы.

## Стек и runtime

- **Язык(и):** `<Python 3.12 / TypeScript / Go / …>`
- **Фреймворки:** `<FastAPI / React / Next.js / …>`
- **БД:** `<Postgres / SQLite / нет>`
- **Менеджер пакетов:** `<uv / poetry / pip / npm / pnpm>`
- **Тесты:** `<pytest / vitest / jest>` (команда: `<uv run pytest>`).
- **Линтер/форматер:** `<ruff / eslint / prettier>` (команда: `<uv run ruff check>`).
- **Type-checker:** `<mypy / tsc>` (команда: `<mypy . --no-incremental>`).

## Раскладка репозитория

```
<repo>/
├── src/                # основной код
├── tests/              # тесты
├── docs/               # документация
└── …
```

Опиши важные директории и их роли.

## Регистрационные / shared-файлы

> Это файлы, которые редактируются ОЧЕНЬ многими задачами одновременно
> (registry-файлы вроде `webapp.py` с роутерами, `App.jsx` с роутами,
> `pyproject.toml` со скриптами/зависимостями). Конфликты между задачами
> в этих файлах — частая причина падения merger'а.
>
> orchX-planner получит этот список и постарается ставить правки таких
> файлов **последовательно**, а не параллельно. Если твоего проекта это
> не касается — просто оставь раздел пустым.

- `<src/main.py>` — регистрация роутеров/команд.
- `<package.json>` — scripts/зависимости.
- …

## Конвенции коммитов

- Стиль: `<conventional commits / 50/72 / прозрачные сообщения>`.
- Префиксы: `<feat / fix / docs / refactor / test / chore>`.
- Язык сообщения: `<en / ru>`.
- Pre-commit хуки: `<да, обязательны / нет>`.

## Документация

- Где живёт: `<docs/>`.
- Когда обновлять: `<при добавлении public API / при breaking change / …>`.
- Формат: `<markdown / mkdocs / docusaurus>`.

## Запреты для агентов

> Эти tool'ы агенты вызывают по инерции, но в orchX-runtime их нет — они
> только тратят step budget. Перечисли все tool-name-prefix'ы / suffix'ы,
> которые твой проект использует в обычной разработке, но которые
> orchX-воркеру **не нужны**.

- MCP-серверы: `<example_*, foo_execute, …>` — не существуют в worker runtime.
- Сторонние LSP/skill'ы: `<…>`.
- HTTP к продакшен-инфре: запрещено напрямую — только через mock'и в тестах.

## Опасные операции

- Миграции БД: `<команда / процесс>`. После миграции — обязательный
  checkpoint в plan'е.
- Массовые rename'ы: `<выноси в отдельную задачу с file_scope=glob>`.
- Удаление публичного API: только через deprecation period.

## Локальные особенности

> Любые подсказки, которые помогут агенту работать эффективнее: «у нас
> heavy import в `__init__.py`, поэтому `python -c 'import foo'` падает —
> используй `python -c 'import importlib.util; …'` для проверки», «тесты
> требуют `DATABASE_URL=…`», «фронт собирается через vite, dev-server
> на 3001», и т.п.

- …
